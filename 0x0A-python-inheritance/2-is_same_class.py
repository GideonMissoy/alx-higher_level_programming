#!/usr/bin/python3
"""
Function returns True if the object is exactly an instance of class
"""


def is_same_class(obj, a_class):
    """Returns true if is class, otherwise false"""
    return (type(obj) == a_class)
